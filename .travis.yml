language: python

python: "3.7"
install:
  - pip install -r requirements.txt

script:
  - black -q --check money_bot
  - isort -rc money_bot
  - pylint -E money_bo

after_success:
  - pylint money_bot
